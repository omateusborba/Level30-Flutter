# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/build/
android/.gradle/

# Flutter generated plugin registries
.flutter-plugins
.flutter-plugins-dependencies

# Google Services (never commit credentials)
google-services.json
GoogleService-Info.plist

# Node / Cloudflare Worker (server/) e dashboard Angular (dashboard/)
node_modules/
.wrangler/
.dev.vars
/dashboard/dist/
/dashboard/.angular/

# Backend Spring Boot (backend/)
/backend/target/
/backend/data/

# Segredos — nunca versionados (varre o repo inteiro, nao so /deploy)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keystore
**/local.properties
secrets.*

# Material de apoio (repositório git separado, fora do build)
/agents/

