spring:
  datasource:
    url: jdbc:h2:mem:level30test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true

app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret-0123456789
    access-ttl-seconds: 3600
    refresh-ttl-seconds: 2592000
  ai:
    worker-url: ""
    service-token: ""
  cors:
    allowed-origins: http://localhost:4200
  security:
    auth-rate-limit:
      max-requests: 500   # alto por padrao; o teste de rate limit sobe seu proprio contexto
      window-seconds: 60
  seed:
    enabled: false
